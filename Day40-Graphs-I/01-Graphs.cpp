#include <iostream>
using namespace std;

int main(void)
{

    return 0;
}

/*
    Graphs:
    - A graph is a non-linear data structure consisting of nodes and edges. It is like a network of interconnected nodes. Graphs are used to represent relationships between different entities, such as social networks, computer networks, and transportation systems.
    - Nodes are also called vertices.
    - Edges are the connections between the nodes.
    - Graphs can be directed or undirected.
        - Directed graphs have edges with a direction, while undirected graphs have edges without a direction.
    - Graphs can also be weighted or unweighted.
        - Weighted graphs have edges with weights, while unweighted graphs have edges without weights.
    - Graphs can be represented in various ways, such as adjacency matrix, adjacency list, and edge list.

    Example of a graph:
        A --- B
        |     |
        C --- D

    In this example, we have 4 nodes (A, B, C, D) and 4 edges (A-B, A-C, B-D, C-D). This is an undirected graph because the edges do not have a direction. It is also an unweighted graph because the edges do not have weights.

    Types of graphs:
    - Directed Graph (Digraph): A graph where edges have a direction. For example,
        A --> B
        |     |
        C --> D
    - Undirected Graph: A graph where edges do not have a direction. For example,
        A --- B
        |     |
        C --- D
    - Weighted Graph: A graph where edges have weights. For example,
        A ---5--- B
        |         |
        3         2
        C ---1--- D
    - Unweighted Graph: A graph where edges do not have weights. For example,
        A --- B
        |     |
        C --- D
*/